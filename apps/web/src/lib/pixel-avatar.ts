/** Single import site for the pixel-art renderer (keeps the client bundle to the browser-safe surface). */
export { renderPortrait, paintToCanvas, PORTRAIT_W, PORTRAIT_H } from '@opersona/pixel-avatar';
export { talkingFrames, avatarStateFrames } from '@opersona/pixel-avatar';
export { walkerFramesV2, WALKER_W, WALKER_H } from '@opersona/pixel-avatar';
